# Atividade Avaliativa 02

### Questão 01

Para trabalhar com matrizes em Python, pacote mais popular e eficiente é o **NumPy**.
**[RESPOSTA]** Verdadeiro

> Numpy é uma biblioteca robusta, escrita em C, que permite trabalhar com matrizes de forma eficiente, além de oferecer diversas funções matemáticas e estatísticas.

### Questão 02

Assinale se a afriamção é veradeira ou falsa.

- Os elementos em um vetor em Python são indexados **a partir de zero**.
  **[RESPOSTA]** Verdadeiro
  > A indexação em Python começa em 0.

### Questão 03

A função `sns.heatmap()` do seaborn é uma excelente ferramenta para visualizar matrizes de covariância e correlação. O mapa de calor usa cores para representar a magnitude dos valores, facilitando a identificação de variáveis que covariam ou correlacionam fortemente (positiva ou negativamente).
**[RESPOSTA]** Verdadeiro

> O seaborn é uma biblioteca de visualização de dados em Python, construída sobre o matplotlib, que oferece funções de alto nível para criar gráficos estatísticos atraentes e informativos. A função sns.heatmap(), especificamente, é ideal para visualizar matrizes de covariância e correlação, pois utiliza uma escala de cores para representar a magnitude dos valores, permitindo identificar rapidamente padrões e relações entre as variáveis.

### Questão 04

A matriz de correlação, obtida através do método .corr()` de um DataFrame do pandas, possui valores na sua diagonal principal sempre iguais a zero, indicando que a correlação de uma variável consigo mesma é sempre perfeita e positiva (uma correlação de 1).
**[RESPOSTA]** Falso

> Na verdade, a matriz de correlação possui valores na sua diagonal principal sempre iguais a um, indicando que a correlação de uma variável consigo mesma é sempre perfeita e positiva (uma correlação de 1).

### Questão 05

A biblioteca pandas realmente fornece os métodos `.mean()`, `.median()` e `.mode()` para calcular as medidas de tendência central em Series e DataFrames.
**[RESPOSTA]** Verdadeiro
